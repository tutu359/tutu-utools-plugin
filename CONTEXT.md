# tutu 工具箱

一个承载多个个人工具的 uTools 插件，慢慢生长、逐步补充功能。

## Language

**模板模式**:
uTools 的官方轻量插件形态：无 `main` 页面，工具以 `window.exports` 列表模式实现，交互全部发生在搜索框与其下方列表。本插件的基础形态（见 ADR 0001）。
_Avoid_: 无 UI 模式、模板插件

**标签页**:
工具进入后挂在搜索框上的可关闭标签，用户在标签后继续输入，即维护者所说的「带前缀继续输」。
_Avoid_: 子输入框（面向实现）、chip

**插件（壳）**:
承载所有工具的 uTools 插件本体，本身只是容器。
_Avoid_: 程序、应用

**工具（Tool）**:
可通过关键字唤起的功能单元，有自己的交互方式（独立界面，或 uTools 输入框内的交互）。
_Avoid_: 功能、模块、feature

**工具箱**:
这个插件整体的昵称，不是一种工具。

**哈希计算**:
把一段文本实时计算为多种算法哈希值并逐行展示的工具。
_Avoid_: 取哈希、hash 工具

**快速 Shell**:
在 uTools 输入框内直接输入并执行 shell 命令的工具；命令有输出时在搜索框下方展开显示。
_Avoid_: 终端、命令行

# tutu 工具箱

一个承载多个个人工具的 uTools 插件，慢慢生长、逐步补充功能。

## Language

**插件（壳）**:
承载所有工具的 uTools 插件本体，本身只是容器。
_Avoid_: 程序、应用

**工具（Tool）**:
可通过关键字唤起的功能单元，有自己的交互方式（独立界面，或 uTools 输入框内的交互）。
_Avoid_: 功能、模块、feature

**工具箱**:
这个插件整体的昵称，不是一种工具。

**哈希计算**:
把一段文本实时计算为多种算法哈希值并逐行展示的工具。
_Avoid_: 取哈希、hash 工具

**快速 Shell**:
在 uTools 输入框内直接输入并执行 shell 命令的工具；命令有输出时在搜索框下方展开显示。
_Avoid_: 终端、命令行

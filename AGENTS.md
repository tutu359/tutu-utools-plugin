## Agent skills

### Issue tracker

本仓库使用 GitHub Issues 跟踪 issues 和 specs。详见 `docs/agents/issue-tracker.md`。

### Triage labels

本仓库使用默认的五角色 triage 标签词汇。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单上下文布局，使用根目录 `CONTEXT.md` 和 `docs/adr/`。详见 `docs/agents/domain.md`。
